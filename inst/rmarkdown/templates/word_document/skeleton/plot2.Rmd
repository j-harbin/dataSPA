---
title: "plot"
output: html_document
---

```{r, echo=FALSE}
df <- plotSPA(salary=index2, which="salaryAllocation", theme=unique(index2$theme), dataframe = TRUE)
res <- try(plotSPA(salary=index2, which="salaryAllocation", theme=unique(index2$theme), item=names(df)[j]), silent=TRUE)
```
