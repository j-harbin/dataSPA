---
title: "Monitoring Detail Fact Sheet"
output:
  html_document: default
  pdf_document: default
---

<a href ="file://dcnsbiona01a/BIODataSvc/IN/MSP/PowerBI-Projects/dataSPA/outputs/hub/dataSPA.html"><img src="home.png" width="2%"></a>


```{r, echo=FALSE}
years <- unique(index$fiscal_year)
```

```{r, echo=FALSE, comment=NA, results="asis"}
file2 <- paste0(path,"dataSPA_om.rds")
if (file.exists(file2)) {
cat(paste0("Data pulled for this report was obtained on ", getData(type="om_date", age=30, cookie=cookie, path=path)))
}
```

**Title**

```{r, echo=FALSE, comment=NA, results='asis'}
cat(paste0(unique(index$project_title)))
```

**Project ID**

```{r, echo=FALSE, comment=NA,results='asis'}
cat(paste0(unique(index$project_id)))
```

**Overview**

```{r, echo=FALSE, comment=NA, results='asis'}
file <- "//dcnsbiona01a/BIODataSvc/IN/MSP/PowerBI-Projects/dataSPA/inputs/Ecological Pillar Objectives 20 April 2023.docx"
ID <- unique(index$project_id)
if (file.exists(file)) {
cat(highlightOverview(om=index,id=ID))
} else {
  cat(paste0(unique(index$overview)))
}
```

<center>
```{r pressure, echo=FALSE, fig.cap=" ", out.width = '70%'}
if (file.exists(file)) {
highlightOverview(om=index, id=unique(index$project_id), legend=TRUE)
}
```
</img></center>

**Deliverables**

```{r, echo=FALSE, comment=NA,results='asis'}
d <- unique(index$deliverables)
if (length(unique(d)) == 1 && unique(d) == 0) {
  cat(paste0("There are no identified deliverables for this project."))
  #print(paste0(unlist(strsplit(unique(index$milestones)[1], "|-----|", fixed=TRUE))))
} else {
    deliverables <- NULL
    if (!(length(unique(d))) == 1) {
      for (Y in seq_along(years)) {
        ii <- index[which(index$fiscal_year == years[Y]), ]
        deliverables[[Y]] <- unique(ii$deliverables)
      }
      deliverables <- unlist(deliverables)
      if (length(d) == length(deliverables)) {
      for (Y in seq_along(years)) {
      if (deliverables[Y] == 0) {
        deliverables[Y] <- "No identified deliverables for this year."
      }
      cat(paste0(years[Y], ": \n"))
      cat(paste0(unlist(strsplit(deliverables[Y], "|-----|", fixed=TRUE)), "\n\n"))
      #cat(paste0(years[i], " : ", deliverables[i]))
      cat("  \n")
      cat("  \n") 

      }
      
      } else {
       # Some are identified to be the same
        
        for (D in seq_along(d)) {
          dd <- which(deliverables == d[D])
          
          if (length(dd) == 1) {
            years <- unique(index$fiscal_year[which(index$deliverables == deliverables[dd])])
            cat(paste0(years, ": \n"))
            if (!(deliverables[dd] == 0)) {
            cat(paste0(unlist(
              strsplit(deliverables[dd], "|-----|", fixed = TRUE)
            ), "\n\n"))
            } else {
              cat(paste0("There are no milestones identified for this year."))
            }
            cat("  \n")
            cat("  \n")
          } else {
            # This is where the change occurs
            YEARS <-
              unique(index$fiscal_year[which(index$deliverables == deliverables[dd[1]])])
            
            if (!(deliverables[dd[1]] == 0)) {

            cat(paste0(paste0(YEARS, collapse = " and "), " have the same deliverables(s): \n"))
            
             cat(paste0(unlist(
              strsplit(deliverables[dd[1]], "|-----|", fixed = TRUE)
            ), "\n\n"))
            } else {
              # More than one year has no deliverables
               cat(paste0(paste0(YEARS, collapse = " and "), " have no identified deliverables(s) \n"))
              
            }
            cat("  \n")
            cat("  \n")

          }
        }
        
    }
    } else {
      
      if (grepl("\t\t\t\t", d)) {
        d <- sub("\t\t\t\t", "", d) # Remove first tab
        if (grepl("\t\t\t\t\t\t",d)) {
          d <- sub("[\t]+","", d)
        }
      }
      cat(paste0("All years (", paste0(years, collapse = ","), ") have the same deliverable(s):"))
      cat("  \n")
      cat("  \n")
      cat(paste0(d))
    }
}
```

**Milestones**

```{r, echo=FALSE, comment=NA,results='asis'}
years <- unique(index$fiscal_year)
m <- unique(index$milestones)
if (length(unique(m)) == 1 && unique(m) == 0) {
  cat(paste0("There are no identified milestones for this project."))
} else {
    milestones <- NULL
    if (!(length(unique(m))) == 1) {
      for (Y in seq_along(years)) {
        ii <- index[which(index$fiscal_year == years[Y]), ]
        milestones[[Y]] <- unique(ii$milestones)
      }
      milestones <- unlist(milestones)
      if (length(m) == length(milestones)) {
      for (M in seq_along(years)) {
      if (milestones[M] == 0) {
        milestones[M] <- "No identified milestones for this year."
      }
      cat(paste0(years[M], ": \n"))
      cat(paste0(unlist(strsplit(milestones[M], "|-----|", fixed=TRUE)), "\n\n"))
      cat("  \n")
      cat("  \n") 
      }
      } else {
        # At least one year has the same milestone, but not all
        for (M in seq_along(m)) {
          mm <- which(milestones == m[M])
          
          if (length(mm) == 1) {
            years <- unique(index$fiscal_year[which(index$milestones == milestones[mm])])
            cat(paste0(years, ": \n"))
            if (!(milestones[mm] == 0)) {
            cat(paste0(unlist(
              strsplit(milestones[mm], "|-----|", fixed = TRUE)
            ), "\n\n"))
            } else {
              cat(paste0("There are no milestones identified for this year."))
            }
            cat("  \n")
            cat("  \n")
          } else {
            # This is where the change occurs
            YEARSM <-
              unique(index$fiscal_year[which(index$milestones == milestones[mm[1]])])
            
            if (!(milestones[mm[1]] == 0)) {

            cat(paste0(paste0(YEARSM, collapse = " and "), " have the same milestone(s): \n"))
            
             cat(paste0(unlist(
              strsplit(milestones[mm[1]], "|-----|", fixed = TRUE)
            ), "\n\n"))
            } else {
              # More than one year has no milestones
               cat(paste0(paste0(YEARSM, collapse = " and "), " have no identified milestone(s) \n"))
              
            }
            cat("  \n")
            cat("  \n")

          }
        }

      }
      
    } else {
      # Right now, this only does this for 1 milestone
      if (grepl("\t\t\t\t", m)) {
        m <- sub("\t\t\t\t", "", m) # Remove first tab
        if (grepl("\t\t\t\t\t\t",m)) {
          m <- sub("[\t]+","", m)
        }
      }
      cat(paste0("All years (", paste0(years, collapse = ","), ") have the same milestone(s):"))
      cat("  \n")
      cat("  \n")
      cat(paste0(m))
    }
}
```


**Objectives**

```{r, echo=FALSE, comment=NA,results='asis'}
if (unique(index$objectives) == 0) {
  cat(paste0("There are no identified objectives for this project."))
} else {
cat(paste0(unique(index$objectives)))
}


```

**Status**

```{r, echo=FALSE, comment=NA, results='asis'}
years <- unique(index$fiscal_year)
status <- NULL
if (!(length(unique(index$status)) == 1)) {
  for (Y in seq_along(years)) {
    ii <- index[which(index$fiscal_year == years[Y]),]
    status[[Y]] <- unique(ii$status)
  }
  status <- unlist(status)
  cat(paste0(years," : ", status, collapse=", "))
} else {
  cat(paste0(unique(index$status)), " for all project years")
}
```

**Lead Contact (s)**

```{r, echo=FALSE, comment=NA,results='asis'}
cat(paste0(unique(index$lead_staff)))
```

**Plots**

A number of assumptions exist for plots 3,4, and 6 including:

1. The calculated salaries use the median salaries for the year specific to the project of each given job classification. 
2. If a classification level is not given in the project planning tool, a level of PC-2 is assumed. 
3. If the salary for a given year of the project is not available in the public DFO pay scale website, the closest year is used.
4. When overtime amounts are considered in the total, it does not take into account that overtime can sometimes be 1.5 or 2X the pay.
5. When a person with job classification EX is associated with a project, they are not included in total salary.
6. For salary predictions, overtime is not accounted for and it is assumed that each classification level worked the same amount of hours as the previous year
7. The PPT was only mandated in the Maritimes region 2 years ago

```{r, echo=FALSE}
ID <- unique(index$project_id)
```

```{r, echo=FALSE, fig.cap="Figure 1: Bar chart showing the amount of O&M money ($) invested into the project. Graph color coded by funding type and show the number of deliverables and milestones per year in blue and red respectively"}
plotSPA(om=index, id=ID, which="omBar")
```

```{r, echo=FALSE, fig.cap="Figure 2: Line graphs representing changes in O&M investment for categories over the years for a specific project. Average rate of change per year for each category, where applicable, displayed on the graph in red"}
plotSPA(om=index, id=ID, which="omAllocation")
```

```{r, echo=FALSE, fig.cap="Figure 3: Bar graph representing amount of salary money ($) using the median values of a specific job classification. Graph color coded by funding type indicated by the legend."}
plotSPA(salary=index2, id=ID, which="salaryBar")
```

```{r, echo=FALSE, fig.cap="Figure 4: Line graphs representing changes in Salary investment for different classifications over the years for a specific project. Average rate of change per year for each category, where applicable, displayed on the graph in red"}

res <- try(plotSPA(salary=index2, id=ID, which="salaryAllocation"), silent=TRUE)
```

```{r, echo=FALSE, fig.cap="Figure 5: Bar graph representing percentage (%) of Indeterminate vs Non-indeterminate employees for project years.Number of staff shown on the figure in red"}
plotSPA(om=index, salary=index2, id=ID, which="indeterminate")
```


```{r, echo=FALSE, fig.cap="Figure 6: Bar graph showing gap in funding (red) if funding stayed the same and salary cost continued to increase. The rate of increase is determined using linear regression. Actual years are shown on the left of the dashed line, and predicted years are on the right of the dashed line. Total gap amount rounded to the nearest dollar shown in red text."}

plotSPA(om=index, salary=index2, which="predictSalary", id=ID)

```
