---
title: "plot"
output: html_document
---

```{r, echo=FALSE}
df <- plotSPA(salary=index2, which="salaryAllocation", division = DIV, dataframe = TRUE)
res <- try(plotSPA(salary=index2, which="salaryAllocation", division=DIV, item=names(df)[j]), silent=TRUE)
```
