---
title: "plot"
output: html_document
---

```{r, echo=FALSE}
df <- try(plotSPA(salary=index2, which="salaryAllocation", functionalGroup=unique(index2$functional_group), dataframe = TRUE), silent=TRUE)
res <- try(plotSPA(salary=index2, which="salaryAllocation", functionalGroup=unique(index2$functional_group), item=names(df)[j]), silent=TRUE)
```
